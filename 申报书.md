# 项目申报书

**项目名称**：moon-httpsig — MoonBit RFC 9421 HTTP Message Signatures 实现
**模块**：`xiguaAp6y3/moon-httpsig`（v0.1.0，Apache-2.0）
**仓库**：https://github.com/xiguaAp6y3/moon-httpsig
**作者**：张国迅（GitHub: xiguaAp6y3）｜ 语言：MoonBit（wasm-gc / js / native 三目标）

---

## 一、要解决的问题

MoonBit 生态缺少 RFC 9421（HTTP Message Signatures）标准实现。本项目从零补齐该空白，不依赖 JWT/HTTP 框架/TLS/通用密码学库，也不基于旧版 Cavage Draft。

## 二、应用场景

面向 API 与 Webhook 的**请求完整性**与**消息签名**：服务间调用、Webhook 投递、响应签名（绑定关联请求）、防重放与时效校验。

## 三、技术路线

HTTP 消息模型 → RFC 9651 Structured Field 子集 → Covered Component 解析 → Signature Base 逐字节构造 → HMAC-SHA256 → Verification Policy（算法白名单/时间/required/keyid/nonce/tag/Content-Digest 绑定/多签名策略）→ Signer/Verifier 与用户界面。

## 四、交付物

- **核心库**：消息模型、SF 子集、组件解析、签名基、HMAC、策略、Signer/Verifier（约 4300 行手写 MoonBit）；
- **CLI** `httpsig-tool`：11 个子命令，稳定 JSON 输出；
- **HTTP/1.1 Adapter**：文本解析/格式化、附加签名头、RFC 9530 Content-Digest 绑定；
- **6 个可运行示例**：签名/验签/响应签名/多签名/防重放/Content-Digest；
- **复现脚本**：`verify_all.ps1` 一键验证 + 行数统计 + RFC fixture 生成/校验。

## 五、工程验证

- **101 个具名测试**、91 个表驱动案例、**1100 组确定性属性测试**、31 项负面安全矩阵；
- **RFC 官方向量**：Appendix B.2.1–B.2.6 签名基逐字节通过，B.2.5 HMAC 与官方值一致；
- **三目标**：wasm-gc / js / native 的 check/build/test 全部通过，0 错误、0 警告；
- 全链路 `verify_all.ps1` 一键复现通过。

## 六、说明

算法仅内置 hmac-sha256（其余识别但返回 AlgorithmNotAllowed，不假验签）；RFC 9651 为所需子集；不支持远程 Key Resolver/JWKS/DID（核心库不联网）。以上数据均来自实际运行结果。
