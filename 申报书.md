# 项目申报书

**项目名称**：moon-httpsig — MoonBit RFC 9421 HTTP Message Signatures 实现
**参赛者**：张国迅（GitHub：xiguaAp6y3）
**联系方式**：2812652516@qq.com
**GitHub 仓库链接**：https://github.com/xiguaAp6y3/moon-httpsig.git
**项目方向**：MoonBit 密码学 / HTTP 消息完整性基础库，面向 API 与 Webhook 请求签名与验签
**是否为移植项目**：否 —— 从零实现，仅参考公开标准文本

## 项目简介

moon-httpsig 是使用 MoonBit 从零实现的 [RFC 9421]（HTTP Message Signatures）标准库，填补 MoonBit 生态在该标准上的空白。面向 MoonBit 库开发者与应用开发者，为服务间调用、Webhook 投递、响应签名等场景提供请求完整性与消息签名基础件。交付内容覆盖 HTTP 消息模型、RFC 9651 Structured Field 子集、covered component 解析、签名基逐字节构造、HMAC-SHA256 签名/验签与完整验证策略。

## 核心功能范围

- **HTTP 消息模型**：保序、可含同名多值、区分 Header/Trailer 的请求与响应模型；
- **RFC 9651 子集**：Dictionary / Inner List / Item / Parameters 解析与规范序列化；
- **组件解析**：`@method`、`@target-uri`、`@authority`、`@scheme`、`@request-target`、`@path`、`@query`、`@query-param`、`@status` 等派生组件，以及含 `sf`/`bs`/`key`/`tr`/`req` 参数的字段组件；
- **签名基**：按 RFC 9421 §2.5 逐字节构造；**HMAC-SHA256**：按 RFC 2104 构造，常量时间比较；
- **验证策略**：算法白名单、时间（created/expires/最大年龄/时钟偏移）、required components、keyid、nonce、tag、Content-Digest 绑定、多签名策略；
- **CLI 与适配**：`httpsig-tool`（11 个子命令，稳定 JSON 输出）、HTTP/1.1 文本适配器、6 个可运行示例；
- **工程验证**：101 个具名测试、91 个表格案例、1100 组确定性属性测试、RFC Appendix B 官方向量逐字节通过、wasm-gc / js / native 三目标 check/build/test 全部通过。

## 移植或参考说明

- **参考标准**：RFC 9421（核心）、RFC 9651（Structured Field Values）、RFC 2104（HMAC）、RFC 9530（Content-Digest）、RFC 9110（HTTP Semantics）；
- **实现方式**：从零手写（约 4300 行 MoonBit），不复制、不翻译任何第三方 HTTP Message Signatures 语言实现；唯一运行时依赖 `gmlewis/sha256`（Apache-2.0）作为成熟 SHA-256 原语；
- **边界与简化**：仅内置 hmac-sha256，其余算法识别但返回 `AlgorithmNotAllowed`、不假验签；核心库不联网，不支持远程 Key Resolver / JWKS / DID；
- **License**：Apache-2.0，详见仓库 LICENSE 与 docs/ 目录。

[RFC 9421]: https://www.rfc-editor.org/rfc/rfc9421.html
