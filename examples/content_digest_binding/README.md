# content_digest_binding

Shows that a signature alone does not protect the body. With a valid and
covered `Content-Digest`, body tampering and digest tampering are detected.

Run:

```sh
moon run examples/content_digest_binding
```
