# multiple_signatures

Puts two signatures on one message. One verifies and one does not; the
`VerificationReport` shows both, and the `SpecificLabel` policy accepts only
the valid label.

Run:

```sh
moon run examples/multiple_signatures
```
